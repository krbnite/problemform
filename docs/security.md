# Security 
Current MVP assumes trusted inputs
* No tool execution exists yet
* Prompt injection defenses become important when:
    * MCP is added
    * tools are added
    * web access is added
    * code execution is added